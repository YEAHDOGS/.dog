KNOWN DEVIATION vs dog.py (reference): dog.py treats any unknown directive as ignorable per SPEC 2 / RULES R1.4, so it silently ignores the truncated directive and parses on (lang defaults / dict unset). dog.js rejects truncated known-directive names as hard errors because abbreviated directives (bag log D1) are NOT in the spec and silently mis-resolving them would violate the no-guessing rule. Reconcile dog.py once the founder rules on D1.